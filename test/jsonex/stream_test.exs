defmodule JsonexStreamTest do
  use ExUnit.Case
  doctest Jsonex

  # test "string parts begin empty" do
  #   assert ["\"", "null\""] == "null"
  # end

  # test "string parts end empty" do
  #   assert ["\"null", "\""] == "null"
  # end

  # test "null string 3 parts" do
  #   assert ["\"", "null", "\""] == "null"
  # end
end
